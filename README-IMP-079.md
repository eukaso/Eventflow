# IMP-079 — Sprint 10 release candidate

IMP-079 prepares the locally accepted Sprint 10 API-completion scope as the EventFlow 1.1.0 release candidate.

The package adds the Sprint 10 acceptance report and candidate release notes, linked to the 32-row executable evidence catalogue and single controlled Migration-status deferral. Executable release-discipline checks retain plugin version `1.1.0-dev`, schema version 15, and the `[Unreleased]` changelog until CI promotion is approved.

The target tag is `v1.1.0-api-completion` and the explicit input release is `v1.0.0-delivery-adapters`. Stable metadata, changelog closure, merge to `main`, and tagging remain blocked pending a green GitHub Actions PHP 8.2/8.3 matrix for this candidate commit.
